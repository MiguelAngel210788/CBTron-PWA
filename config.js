/* Configuración pública de la carcasa instalable.
 * La URL del backend se puede dejar vacía: la carcasa la solicitará en pantalla
 * y la guardará únicamente en este dispositivo.
 */
window.CBTRON_PWA_CONFIG = Object.freeze({
  version: '0.1.23',
  gasWebAppUrl: ''
});
